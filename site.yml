---
- name: Deploy Flask on webservers
  hosts: webservers
  become: yes
  vars_files:
    - vars/main.yml
  roles:
    - flask

- name: Configure nginx load balancer
  hosts: nginx
  become: yes
  vars_files:
    - vars/main.yml
  roles:
    - nginx
